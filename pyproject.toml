[project]
name = "run-atopile"
version = "0.2.0"
description = "A Github Action to run atopile"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = ["httpx>=0.28.1", "ruamel-yaml>=0.18.10", "semver>=3.0.4"]

[tool.uv]
required-version = ">=0.9.0"
